# ============================================================
# VPINN Express — 依赖清单
# ============================================================
# Streamlit Cloud 当前运行 Python 3.14，
# 可用的 CPU-only PyTorch 版本为 2.9.x / 2.10.x。
# --extra-index-url 指向 PyTorch 官方 CPU-only 轮子仓库。
# ============================================================

--extra-index-url https://download.pytorch.org/whl/cpu

# ── 核心框架 ──
streamlit>=1.32.0
torch==2.10.0+cpu             # CPU-only, 兼容 Python 3.14

# ── 科学计算 ──
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0

# ── 可视化 ──
plotly>=5.18.0
